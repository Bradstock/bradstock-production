---
label: Gallery
hide_body: true
fields:
- name: gallery_field_group
  type: field_group_list
  fields:
  - name: gallery_image
    type: file
    config:
      maxSize: 64
    label: Gallery Image
  config:
    min: 
    max: 
    labelField: 
  label: Gallery Field Group
