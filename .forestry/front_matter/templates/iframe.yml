---
label: Iframe
hide_body: true
fields:
- type: text
  name: component
  label: component
  hidden: true
  default: iframe
- name: url
  type: text
  config:
    required: false
  label: URL
  description: enter the form url, if using a google form make sure to get the correct
    embed code which will allow urls to be embedded in the site. this should also
    come with height.
- name: iframe_height
  type: text
  config:
    required: false
  label: Iframe Height
